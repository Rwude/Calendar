import { CalendarTooltipDirective } from './calendar-tooltip.directive';

describe('CalendarTooltipDirective', () => {
  it('should create an instance', () => {
    const directive = new CalendarTooltipDirective();
    expect(directive).toBeTruthy();
  });
});
